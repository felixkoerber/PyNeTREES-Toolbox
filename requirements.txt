# Reproduces the environment this project was developed and tested against
# (Python 3.13, Windows). Installs everything: core + plotting (pyvista,
# matplotlib) + dev/test tooling (pytest).
#
#   pip install -r requirements.txt
#   pip install -e .          # install pytrees itself in editable mode
#
# pyproject.toml defines the same dependencies as proper extras, if you'd
# rather install a subset:
#   pip install -e .                # core only (numpy, scipy)
#   pip install -e ".[plot]"        # + pyvista, matplotlib
#   pip install -e ".[dev]"         # + pytest
#   pip install -e ".[plot,dev]"    # everything (equivalent to this file)

# --- core ---
numpy==2.4.6
scipy==1.17.1
pandas==3.0.5

# --- plotting (optional 'plot' extra) ---
pyvista==0.48.4
vtk==9.6.2          # pulled in by pyvista; pinned here since it's the heavy one
matplotlib==3.10.7

# --- interactive Jupyter widgets (examples/plot.ipynb only, not needed for plotting.py itself) ---
trame==3.13.2
trame-vtk==2.11.15
trame-vuetify==3.2.5
ipywidgets==8.1.8

# --- dev / test (optional 'dev' extra) ---
pytest==9.1.1
